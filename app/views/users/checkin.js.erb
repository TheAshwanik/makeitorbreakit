var newDiv = document.createElement("div")
var checkinDiv = document.querySelector("#status<%=@habitid%>")

newDiv.innerHTML = "<%= j render('newstatus').chomp.html_safe %>"

checkinDiv.prepend(newDiv)
